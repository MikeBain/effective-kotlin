var makeMeImmutable = "The only constant is change"

var makeMeAnImmutableList = mutableListOf("first", "second", "third")

var makeMeAnImmutableMap = mutableMapOf("key" to "value", "foo" to "bar")

fun main(args: Array<String>) {
  println(makeMeImmutable)
  println(makeMeAnImmutableList)
  println(makeMeAnImmutableMap)
}
